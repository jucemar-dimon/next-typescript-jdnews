import styles from "./styles.module.scss";

export function SubscrebeButton() {
  return (
    <button type="button" className={styles.subscribeButton}>
      Subscribe now
    </button>
  );
}
